{
  "scripts": {
    "dev": "bottender dev",
    "dev-server": "nodemon server.js",
    "start": "cross-env NODE_ENV=production node handler.js",
    "webhook": "npm run webhook:messenger && npm run webhook:telegram",
    "webhook:messenger": "npx bottender messenger webhook set",
    "webhook:telegram": "npx bottender telegram webhook set",
    "webhook:whatsapp": "npx bottender whatsapp webhook set"
  },
  "dependencies": {
    "aws-serverless-express": "^3.4.0",
    "body-parser": "^1.19.0",
    "bottender": "latest",
    "cross-env": "^6.0.3",
    "express": "^4.17.1",
    "serverless-http": "^2.3.0"
  },
  "devDependencies": {
    "nodemon": "^2.0.1"
  }
}
